# Capacitor 原生壳构建环境变量样例
#   复制为 .env.native.local（被 .gitignore · 不入库）
#   vite build --mode native 时被读
#
# 必填：API 域名（绝对 URL，原生壳不能用相对路径）
VITE_API_BASE=https://juexue.app

# 可选：标记原生模式（已由 vite.config.ts define 注入，覆盖请取消注释）
# VITE_NATIVE=1
