# 底部导航重设计方案 · 5 Tab

> 状态：⚠️ 待用户确认
> 日期：2026-05-26
> 背景：当前 3 Tab（首页/闻思/复习）随新功能上线已不够用；「修持/班级/我的」隐藏在首页卡片和头像入口，发现性差。

---

## 一、方案总览

### 当前 vs 新设计

```
当前（3 Tab）                    新设计（5 Tab）
─────────────────────            ─────────────────────────────────────
[ 首页 ][ 闻思 ][ 复习 ]         [ 首页 ][ 闻思 ][ 修持 ][ 班级 ][ 我的 ]
```

### Tab 定义

| # | Tab | 路由根 | 图标语义 | 核心用户动作 |
|---|---|---|---|---|
| 1 | **首页** | `/` | 莲花 / 今日 | 看今天是什么日子、快速进入任务 |
| 2 | **闻思** | `/courses` | 法本 / 书 | 读法本、答题复习、写笔记 |
| 3 | **修持** | `/practice` | 念珠 | 记录念诵、修持愿进度、签到打卡 |
| 4 | **班级** | `/class` | 人群 / 共修 | 看班级动态、参加法会共修、讲考 |
| 5 | **我的** | `/me` | 头像 | 个人信息、各类历史记录、设置、通知 |

---

## 二、各 Tab 详细设计

---

### Tab 1 · 首页 `/`

**定位**：仪式感入口，每天打开 App 的第一眼。

**现有内容（保留）**：
- 全屏画报背景（admin 上传月度图 · 渐变兜底）
- 顶部：头像（→ 我的 Tab）+ 藏历日期区域（→ `/calendar`）+ 活动药丸（→ 班级 Tab）
- streak 徽章（🔥 N 天）

**底部卡片区域（调整）**：

原来 4 卡是「导航快捷键」，有了 5 Tab 后定位改为「今日状态摘要」——
告诉用户今天做了什么、还差什么，而不只是导航。

```
┌─────────────────────┐  ┌─────────────────────┐
│ 📖 今日闻思          │  │ 📿 今日修持          │
│ 第3课 · 已读 60%     │  │ 金刚萨埵 108 / 108   │
│ [ 继续阅读 → ]       │  │ ✓ 已完成             │
└─────────────────────┘  └─────────────────────┘
┌─────────────────────┐  ┌─────────────────────┐
│ 🎓 班级动态          │  │ 📅 今日活动          │
│ 3 位同修已打卡       │  │ 周日共修 · 18:00     │
│ 下次共修 周日 18:00  │  │ [ 查看 → ]           │
└─────────────────────┘  └─────────────────────┘
```

- 每张卡点击跳对应 Tab 的相关页
- 卡片内容实时反映今日进度，不只是静态入口
- 无数据时显示引导文案（「今天还没有闻思记录」）

**Tab 触发 TabBar 显示的路由**：仅 `/`（画报风格独立，不做二级嵌套）

---

### Tab 2 · 闻思 `/courses`

**定位**：所有「读 + 思」活动的统一入口。

**合并内容**：
- 现有 `/courses` 法本列表（保留）
- 现有 `/quiz`（复习答题）**合并进来**，作为「复习」入口区块或二级 Tab

**页面结构**：

```
┌─────────────────────────────────────┐
│  闻思                        🔍     │
│                                     │
│  继续学习                           │
│  ┌─────────────────────────────┐    │
│  │ 《菩提道次第广论》           │    │
│  │ 第3课 · 因果不虚             │    │
│  │ ▓▓▓▓▓░░░░░ 60%  [ 继续 → ] │    │
│  └─────────────────────────────┘    │
│                                     │
│  复习  ·  错题 12 道 · SM2 待复习 5  │
│  [ 开始复习 ]  [ 错题本 ]  [ 收藏 ] │
│                                     │
│  所有法本                           │
│  ┌──────┐ ┌──────┐ ┌──────┐        │
│  │广论  │ │前行  │ │入行论│        │
│  └──────┘ └──────┘ └──────┘        │
└─────────────────────────────────────┘
```

**路由归属（TabBar 显示）**：
- `/courses` · `/read/:slug/:lessonId` · `/quiz` · `/quiz-practice` · `/quiz/:lessonId`
- `/mistakes` · `/mistake/:questionId` · `/favorites` · `/sm2-review`
- `/notes` · `/notes/new` · `/notes/:id`（笔记归入闻思）

**现有 `/quiz` Tab 处理**：路由保留，TabBar 高亮改为「闻思」Tab。

---

### Tab 3 · 修持 `/practice`

**定位**：每日功课的操作中心——记录念诵、看修持进度、签到打卡。

**页面结构**：

```
┌─────────────────────────────────────┐
│  修持                               │
│                                     │
│  今日 · 遍数 216  连签 32天  本周 1080│
│                                     │
│  ── 班级修学愿 ──────────────────── │
│  金刚萨埵心咒                        │
│  ▓▓▓▓▓▓░░░░ 6,480 / 10,800         │
│  [ + 打卡 ]                         │
│                                     │
│  普贤行愿                            │
│  ▓▓░░░░░░░░ 700 / 3,500            │
│  [ + 打卡 ]                         │
│                                     │
│  ── 我的修学 ──────────────────────  │
│  [ + 添加修学 ]                      │
│                                     │
│  签到打卡  ←  今日是否已签到徽章     │
│  [ 签到 ]                           │
└─────────────────────────────────────┘
```

**路由归属（TabBar 显示）**：
- `/practice` · `/practice/history` · `/practice/project/:id` · `/practice/:categoryKey`
- `/me/meditations`（观修记录归入修持）

---

### Tab 4 · 班级 `/class`

**定位**：社群维度入口——班级动态、法会共修、讲考。

**当前问题**：`/class/:id` 是二级路由，没有对应的班级根路由，Tab 点击无处落。

**新增根页面 `/class`**（需新建）：

```
┌─────────────────────────────────────┐
│  班级                               │
│                                     │
│  《广论》2026 春季班                  │
│  第 12 周 · 辅导员：法明师兄          │
│  [ 进入班级 → ]                      │
│                                     │
│  ── 法会 / 共修 ──────────────────── │
│  🪷 极乐法会                         │
│  5月28日 – 6月3日 · 进行中           │
│  我的发愿：金刚萨埵 10,800 遍         │
│  [ 查看详情 ]                        │
│                                     │
│  📿 周日共修                         │
│  本周日 18:00 · 3天后                │
│  [ 查看详情 ]                        │
│                                     │
│  ── 班级动态 ──────────────────────  │
│  法明师兄 今日累计 108 遍  👍 随喜    │
│  阿弥师兄 已完成今日签到   👍 随喜    │
└─────────────────────────────────────┘
```

**路由归属（TabBar 显示）**：
- `/class`（新根页）· `/class/:id` · `/class/:id/sessions` · `/class/:id/sessions/:sid`
- `/class/:id/ranking` · `/class/:id/meditations` · `/class/:id/practice-ranking`
- `/events` · `/events/:id`（法会从独立路由归入班级 Tab 高亮）
- `/assemblies/:id`

**活动药丸（首页右上角）**：保留，点击跳到 `/class`（Tab 4）而不是 `/events`。

---

### Tab 5 · 我的 `/me`

**定位**：个人信息、所有历史记录、设置、通知。

**当前问题**：`/profile` 是头像隐藏入口，通知、历史、设置散落各处。

**页面结构**：

```
┌─────────────────────────────────────┐
│  我的                      🔔 通知  │
│                                     │
│  ┌─────┐  行者0001                  │
│  │ 头像 │  姓名 · 法名 · 广州        │
│  └─────┘  已皈依 · 学员 2026001     │
│  [ 编辑个人信息 ]                    │
│                                     │
│  ── 学修记录 ──────────────────────  │
│  讲考记录    >                       │
│  考试成绩    >                       │
│  法会记录    >                       │
│  打卡历史    >  (→ /practice/history)│
│                                     │
│  ── 账号 ──────────────────────────  │
│  设置        >  (→ /settings)        │
│  隐私        >                       │
│  帮助        >                       │
│  退出登录                            │
└─────────────────────────────────────┘
```

**路由归属（TabBar 显示）**：
- `/me`（新根页，替代 `/profile`）· `/profile` → redirect 到 `/me`
- `/me/stats` · `/my/speaking-history` · `/my/exam-grades` · `/my/event-history`
- `/notifications` · `/settings` · `/settings/notifications`
- `/devices` · `/about` · `/help` · `/terms` · `/privacy`
- `/achievement`

---

## 三、TabBar 显示 / 隐藏规则

**显示 TabBar 的路由根**（替代现有 3 个）：

| Tab | 匹配规则 |
|---|---|
| 首页 | `pathname === '/'` |
| 闻思 | `pathname.startsWith('/courses')` 或 `/quiz` 或 `/notes` 或 `/mistakes` 或 `/favorites` 或 `/sm2-review` |
| 修持 | `pathname.startsWith('/practice')` 或 `/me/meditations` |
| 班级 | `pathname.startsWith('/class')` 或 `/events` 或 `/assemblies` |
| 我的 | `pathname.startsWith('/me')` 或 `/profile` 或 `/notifications` 或 `/settings` 或 `/about` 或 `/help` |

**隐藏 TabBar 的路由**（沉浸式阅读 / 播放场景）：
- `/read/:slug/:lessonId`（法本阅读 · 全屏）
- `/meditation/:id`（观修播放 · 全屏）
- `/quiz-practice` · `/quiz/:lessonId`（答题 · 专注模式）

---

## 四、首页头像入口变更

| 原 | 新 |
|---|---|
| 头像 → `/profile` | 头像 → 我的 Tab（`/me`）|
| 通知铃铛（独立）| 通知入口移入「我的」Tab 右上角 |
| 活动药丸 → `/events` | 活动药丸 → 班级 Tab（`/class`）|

---

## 五、与现有代码的差距（待实现）

| 项 | 类型 | 说明 |
|---|---|---|
| TabBar：3 Tab → 5 Tab | 修改 | `TabBar.tsx` + `shouldShowTabBar()` 逻辑 |
| 新增 `/class` 根页 | 新建 | 班级 + 活动 + 动态聚合页 |
| 新增 `/me` 根页 | 新建 | 替代 `/profile` 作为我的 Tab 根 |
| 首页 4 卡改为「今日状态」| 修改 | `HomePage.tsx` 底部卡片内容 |
| 活动药丸点击目标 | 修改 | `/events` → `/class` |
| `/quiz` Tab 高亮 | 修改 | 从独立 Tab → 归属「闻思」Tab |
| 头像跳转 | 修改 | `/profile` → `/me` |

---

## 六、待用户确认的决策点

| # | 问题 | 选项 |
|---|---|---|
| D1 | 复习（/quiz）在闻思 Tab 内是「独立区块」还是「第二个子 Tab」？ | A 区块（滚动到） / B 子 Tab 切换 |
| D2 | 签到打卡放在「修持」Tab 还是独立入口（首页卡片）？ | A 修持 Tab / B 首页卡片 |
| D3 | 「班级」Tab 根页：用户有多个班时，显示主班还是所有班列表？ | A 主班（+ 切班入口）/ B 班级列表 |
| D4 | 首页 4 卡是否改为「今日状态」，还是保留纯导航快捷方式？ | A 今日状态（有进度数据）/ B 保留原样 |
