# 新设计业务逻辑验证计划（场景模拟 v2）

> 创建：2026-06-02
> 对象：五层 SoT（05 战略 / 02 权限 / 06 业务能力 / 08 数据层 / 09 API·页面）
> 方法：把设计文档当成系统，用真实场景**端到端走一遍**，找断点 / 矛盾 / 未定义状态 / 越权口子 / 依赖断裂。
> 适用人：**不写代码的产品经理 + Claude Code**——PM 用白话说"模拟 XX 学员走 YY 流程"，Claude 对照文档推演、列缺口，PM 拍板。
> 与已有报告关系：reports/03（符合大纲）+ reports/04（设计完整）已验"对不对/全不全"两角度；**本计划补第三角度——"端到端流程跑不跑得通"**。旧 `docs/SCENARIO_SIMULATION.md`/`BL_REVIEW.md` 基于旧设计（5-25），仅作方法模板，不直接复用。

---

## 一、五类走查（验证维度）

| # | 走查 | 找什么 |
|---|---|---|
| **D1 端到端场景** | 一条真人旅程逐步追，每步问"有落点吗？数据从哪来到哪去？" | 断点、数据来源缺失 |
| **D2 状态机完整** | 列实体状态转换，查死角 | 未定义转换、回不去的死状态 |
| **D3 权限矩阵** | 每操作 × 每角色 = 能/不能（三端分离铁律） | 越权口子、没人能做的操作 |
| **D4 边界/异常** | 边缘情况有无定义 | 缺口、规则打架 |
| **D5 依赖闭环** | 追跨能力依赖链 | 依赖断裂（A 要 B 数据但 B 没产出） |

---

## 二、产出格式（每条场景统一）

```
### [编号] 场景名
- 白话流程：第1步…第2步…（PM 视角，无术语）
- 走查（Claude）：每步落点（能力X / 表Y / 端点Z）+ 数据来源 + 状态流转
- 结论：✅ 闭环 / ⚠️ 缺口（描述 + 影响）/ ❌ 矛盾（A 处与 B 处冲突）
- 涉及决策：是否触及待定项（A3/F7.2 等）
- 核对结论栏：____（留给 PM 拍板）
```

---

## 三、场景清单（7 批 · 约 55 条）

### 批 1 · 学员主干生命周期（端到端 D1·最高优先）
| 编号 | 场景 | 重点验 |
|---|---|---|
| L-01 | 加行学员全程：入班→选专业→8 学期闻思→92 观修+6 内加行→共修→报数→S8 考试→升学预检→升密法 | 主干闭环、6 硬条件齐不齐 |
| L-02 | 净土学员全程：念佛三选一最宽松路径 | 不卡考试实修（F5）、互斥（C3） |
| L-03 | 入行论学员全程：观修/心咒二选一 | 互斥（C4） |
| L-04 | 学经学员全程：固定功课 + 自选功课 | 自选不计升学（C5/DR-148） |
| L-05 | 自学学员全程：开通→纯完成量→要升学先入正式班 | 边界隔离（不升学/不报数/不关怀） |
| L-06 | 升正科后：多专业并行、主修切换、独立升学 | D16 多专业独立、D9 平等 |

### 批 2 · 特殊学员 + 豁免路径（D1+D4）
| 编号 | 场景 | 重点验 |
|---|---|---|
| S-01 | 盲人学员闻思（听≥2，豁免看/答） | 身份分支判定（DR-92） |
| S-02 | 聋人学员闻思（看≥2，豁免听/答） | 同上 |
| S-03 | 双盲聋（无大纲路径） | 走代行个案（缺口确认） |
| S-04 | 60 岁学员年龄豁免免考（非自动） | 资格 vs 自动满足（能力10） |
| S-05 | 顶礼替代（身体原因→200 万金刚萨埵） | 替代后免补法王祈祷文（DR-94） |
| S-06 | 法王祈祷文欠账→毕业前补 | prayerCount 独立计数（DR-95） |
| S-07 | 跨专业累计共享（加行+另一专业共享内加行） | D14a「果」追溯（programId） |

### 批 3 · 入班/留级/退出/转专业（D1+D2+D4）
| 编号 | 场景 | 重点验 |
|---|---|---|
| E-01 | 邀请码入班（幂等/超次/过期/撤销四情况） | 三态校验、幂等 |
| E-02 | 选专业第 2 学期后想转 | **A3 待定**——验当前行为（应能转，因校验未实施） |
| E-03 | 留级（升学驳回→加新班→新届起修日） | 手动留级、记录保留 |
| E-04 | 留级超次（加行第 3 次 / 净土第 2 次） | **F7.2 缺口**——验系统拦不住 |
| E-05 | 退出专业→记录保留→邀请码回归→衔接 | D15 记录保留、缺席补卡 |
| E-06 | 转专业（退出+加入两步，两段历史独立） | 无平移、跨专业共享仍生效 |
| E-07 | 班级归档（本届清空→归档不删） | D19 |

### 批 4 · 管理角色操作流程（D1+D3）
| 编号 | 场景 | 重点验 |
|---|---|---|
| M-01 | super_admin：建专业×届 + 配升学条件 + 配 LLM | 数据驱动配置（D3） |
| M-02 | subject_admin：学科范围管理、跨班、平台级讲考评分 | 作用域边界 |
| M-03 | class_admin：建班→邀请码→录成绩→升学审核拍板→留级→归档 | 行政权集中、审核流 |
| M-04 | class_tutor：排课→共修打卡→补卡→随堂出题→关怀→建议代行 | **不能录成绩/不能执行代行**（边界） |
| M-05 | 辅助员配对：授 4 项委托权→收回 | 委托权边界（不能改学员数据/认定身份） |

### 批 5 · 横切治理（D1+D4）
| 编号 | 场景 | 重点验 |
|---|---|---|
| G-01 | 代行 5 类（豁免/替代/调整/修正/追溯）+ 撤回 + 学员可见 | AuditLog 台账、撤回=新记录 |
| G-02 | 虚报治理（标记→忏悔→取消资格） | 链完整（E4）、手动解除 |
| G-03 | 共修出勤（online 自助签到 / offline 勾选 / 补卡 / 撤销 / token 时效） | 时效窗口、辅导员不能撤销 |
| G-04 | 报数（日常录入→节点截止冻结快照→核查/修正） | 快照冻结、事后改走 AuditLog |
| G-05 | 考试（随堂 vs 升学考分权→录成绩→开闭卷合格线） | examType 分权、合格线分支（DR-99） |
| G-06 | 传承/灌顶（自动派生/申报/代录→升格→升学核查→密法门禁） | EXISTS 访问控制（DR-44/45） |
| G-07 | 关怀清单（7 类触发→跟进→解除；掉队 5 维） | partial unique、学员不可见 |
| G-08 | 通知触达（9 条定时规则→站内/push/短信三通道） | 规则数据源、critical 静默 |

### 批 6 · 横向验证（D2+D3+D5）
| 编号 | 验证 | 内容 |
|---|---|---|
| ST | 状态机完整性 | cohortStatus / 报数快照 / 邀请码三态 / 升学预检 / 自学 / 关怀 / 特殊身份 / 完成判定（app-kill） 逐个画转换图查死角 |
| PM | 权限矩阵 × 23 职能 | 02 文档 23 职能 × 4 角色逐格核；三端分离铁律（学员端纯消费，admin/coach 登入也无管理操作） |
| DEP | 依赖闭环 | ①升学预检←报数快照←打卡 ②闻思圆满←LessonCompletion+UserAnswer+身份快照 ③关怀←CohortLagSnapshot←各能力 ④代行豁免←AuditLog→预检回查 ⑤传承核查←课程圆满派生 ⑥9 条通知←各能力事件源 |

### 批 7 · 大纲硬规则交叉（D4·与报告 03 联动）
| 编号 | 验证 | 内容 |
|---|---|---|
| GL | 大纲 A1-F8 40 条 × 流程角度 | 复用报告 03 的追溯矩阵，但加"流程能否跑通"维度；重点 2 个 P0 缺口（A3/F7.2）+ 5 处需补强（F2/C5/E1E2/F3.7/A5）在流程里的实际表现 |

---

## 四、推进方式

1. **逐批跑**：批 1（主干生命周期）最高优先——主干通了，系统骨架就立住了。
2. **每批产出**一组场景走查（白话 + 结论 + 核对栏），贴给 PM 核对。
3. **缺口分级**：❌ 矛盾（必修）> ⚠️ 缺口（待决策）> 🔵 待实现（不算设计问题）。
4. **发现 ❌/⚠️ 即登记**到 `OPEN-DECISIONS.md`，与现有 5 条开放项合并管理。
5. **不改设计**——验证只出问题清单，改不改、怎么改由 PM 拍板（同报告 03 原则）。

---

## 五、验收标准（验证"做完"的定义）

```
□ 1. 6 条主干生命周期（批1）全部端到端闭环，无断点
□ 2. 8 个核心状态机（批6 ST）转换完整、无死状态
□ 3. 23 职能 × 4 角色权限矩阵无越权口子、无空操作
□ 4. 6 条跨能力依赖链（批6 DEP）全闭合
□ 5. 所有 ❌ 矛盾已消解或登记
□ 6. 所有 ⚠️ 缺口已登记进 OPEN-DECISIONS 并标优先级
□ 7. 大纲 40 条硬规则在流程角度均有落点（或已知缺口）
□ 8. 三端分离铁律验证通过（学员端纯消费）
```

---

## 六、预判会撞到的问题（头脑风暴·待验证确认）

> 以下是制定计划时预感的"可能有问题处"，作为走查的重点关注，不是结论：

- **E-02/E-04**：A3 选专业锁定、F7.2 留级次数——已知缺口，流程里会现形。
- **G-04 报数 ↔ L-05 自学**：自学不报数，但若自学学员后来入班，历史完成量怎么接进报数？衔接点需验。
- **G-06 传承自动派生 ↔ DR-143 完成纯手动**：课程"听完"现在是手动确认，自动派生传承的触发时机要确认不漏。
- **S-07 跨专业共享 ↔ G-04 报数快照**：跨专业累计的"果"在哪个专业的报数节点结算？口径需验（接 F3.7 起修日口径）。
- **M-04 辅导员 ↔ G-01 代行**：辅导员"建议代行"路由给谁、管理员怎么收到？流程链需验。
- **ST 完成判定 app-kill**：localStorage 续播 + 延迟确认的"未确认会话"，跨场景（换设备/清缓存）边界。
- **批8 后台契约（22-25/38）**：⏸ 不做 UI，但建表+后台——这些与正式能力的数据交互边界（如约修打卡进不进报数）需验。

---

## 变更记录
| 日期 | 内容 |
|---|---|
| 2026-06-02 | 创建验证计划：5 类走查 × 7 批 ≈55 场景 + 验收 8 项 + 7 处预判风险 |
